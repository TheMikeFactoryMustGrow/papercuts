# Papercuts fleet registry — the deployed-repo list fleet-status sweeps.
#
# Rows accumulate ONLY through papercuts-adopt (Mode 2 step 4): a row means
# enablement was verified in that repo in the session that added it.
# fleet-status reads this file; it never edits it.
#
# Seeded 2026-08-23 from docs/FLEET-REVIEW-TODO.md (repos enabled 2026-07-10).
# `verified` = last session that INSPECTED the repo's actual capture data —
# present or absent; the row comment records the finding. It does NOT mean
# "capturing": a silent repo can be verified. fleet-status must read the
# event counts, never treat this date as evidence of capture.

repos:
  - name: Linglepedia
    enabled: 2026-07-10
    verified: 2026-08-23   # 17 logged / 12 resolved
  - name: beancount
    enabled: 2026-07-10
  - name: Bean-Counter-Knowledge
    enabled: 2026-07-10
  - name: bean-counter
    enabled: 2026-07-10
  - name: gix-equity-comp-tool
    enabled: 2026-07-10
  - name: finance-dashboard
    enabled: 2026-07-10
  - name: gix-pipeline
    enabled: 2026-07-10
    # 2026-08-23 fleet-rollout M1 (M1-FLEETYAML approved): local repo (~/code/gix-pipeline,
    # per docs/FLEET-REVIEW-TODO.md) — no GitHub remote in the account; verify on-Mac only.
  - name: imessage-search
    enabled: 2026-07-10
  - name: process-design-plugin
    enabled: 2026-07-10
    verified: 2026-08-23   # enabled, 0 events
  - name: google-workspace-mcp-config
    enabled: 2026-07-10
  - name: partnerships-pipeline-agent
    enabled: 2026-07-10
  - name: autoresearch
    # renamed from karpathy-autoresearch 2026-08-23 (fleet-rollout M1, M1-FLEETYAML approved):
    # the GitHub repo is TheMikeFactoryMustGrow/autoresearch; no repo of the old name exists.
    # 2026-08-23 M4: the GitHub copy had NO machinery (local-clone-only enablement);
    # enabled in the GitHub copy via autoresearch#1, verified in-session.
    enabled: 2026-07-10
  - name: wbr-app
    enabled: 2026-07-10
    # 2026-08-23 fleet-rollout M1 (M1-FLEETYAML approved): local repo (~/code/wbr-app,
    # per docs/FLEET-REVIEW-TODO.md) — no GitHub remote in the account; verify on-Mac only.
  - name: papercuts
    enabled: 2026-07-10
    verified: 2026-08-23   # enabled, 0 events
  - name: gym-plan-analyzer
    enabled: 2026-08-23
    # fleet-rollout batch 1: enablement verified in-session (papercut status
    # project_enabled=true; smoke papercut logged+resolved). Adoption PR:
    # gym-plan-analyzer#1.
  - name: guesty-owners-connect
    enabled: 2026-08-23
    # fleet-rollout batch 1: enablement verified in-session (papercut status
    # project_enabled=true; smoke + gitignore papercuts logged+resolved).
    # Adoption PR: guesty-owners-connect#1.
  - name: decision-review-plugin
    enabled: 2026-08-23
    # fleet-rollout M4: repo was enabled earlier but never registered; snippet
    # refreshed + loop proven in-session (papercut logged+resolved). Adoption
    # PR: decision-review-plugin#15.
  - name: claude-scheduled-tasks
    enabled: 2026-08-23
    # fleet-rollout M4: enablement verified in-session (papercut status
    # project_enabled=true; smoke papercut logged+resolved). Adoption PR:
    # claude-scheduled-tasks#12.
